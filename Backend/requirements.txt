# Django Core
Django==6.0.5
djangorestframework==3.17.1
django-environ==0.12.0
django-filter==25.2
django-cors-headers==4.6.0

# Authentication
djangorestframework-simplejwt==5.5.0
PyJWT==2.9.0

# Database — psycopg3 (modern successor to psycopg2; Django 6 recommends it)
# Binary protocol by default, ~2-3x faster for numeric data, async-ready.
psycopg[binary]==3.3.4

# API Documentation
drf-spectacular==0.29.0
PyYAML==6.0.3
uritemplate==4.2.0

# Encryption
cryptography==44.0.0

# PDF Generation (optional, for invoice PDFs)
reportlab==4.2.2

# Excel Export (optional, for report exports)
openpyxl==3.1.2
pandas==2.3.3

# Other utilities
python-dateutil==2.9.0.post0
requests==2.32.3
urllib3==2.2.3
charset-normalizer==3.3.2
chardet==5.2.0
docutils==0.21.2

# Production (WSGI server)
gunicorn==23.0.0

# Async task queue
celery==5.6.3
redis==5.2.1
django-redis==5.4.0

# Error tracking
sentry-sdk[django]==2.22.0

# Object storage (optional production media)
django-storages[s3]==1.14.4
boto3==1.35.34

# Development & Testing
pytest==8.0.0
pytest-django==4.7.0
pytest-cov==4.1.0
pytest-xdist==3.6.1
factory-boy==3.3.0

twilio==9.2.2
